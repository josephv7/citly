import React from "react";
import Card from "../Card";
import Form from "../Form";

const Login = () => {
  return (
    <Card title={"Login"}>
      <Form />
    </Card>
  );
};

export default Login;
